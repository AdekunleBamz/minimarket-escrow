# Token Symbol Fallback Check

Unset the configured stable token symbol locally and confirm fallback copy is clear.
Record the environment state, displayed token label, and route.
Restore production-like values before smoke tests.
