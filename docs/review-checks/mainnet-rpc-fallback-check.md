# Mainnet RPC Fallback Check

Temporarily fail the primary RPC endpoint and confirm read-only copy remains clear.
Record the fallback endpoint label and affected order reads.
Do not commit private RPC keys or URLs.
