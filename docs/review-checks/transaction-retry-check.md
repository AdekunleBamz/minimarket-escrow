# Transaction Retry Check

Force a failed escrow transaction and confirm retry copy appears without duplicating the order.
Record the error, retry action, and final transaction state.
Do not retry against production orders.
